let numArray = [];

// Deklareriert 'i' als Zählvariable
// Pusht 'i' in das Array und erhöht 'i' um 1
// Solange bis 'i' 10 ist
for(let i = 0; i <= 10; i++) {
    numArray.push(i);
}

console.log(numArray);